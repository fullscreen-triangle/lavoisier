% ============================================================
%  Appendix A: Deductive Flow Chart
%  Source: bounded-phase-space-trajectory.tex, §Deductive Flow Chart
% ============================================================

The following ASCII deductive graph reproduces the dependency structure
from the source derivation, collapsed to the 26 primary Consequences used
in this monograph.
Each node lists which prior Consequences it uses.
Every node is at distance at most 5 from the Axiom.

\begin{center}
\small\ttfamily
\begin{verbatim}
Axiom (i), (ii), (iii)
  |
  +-- Cons. 1  (Poincaré recurrence)         uses (i),(ii)
  |     |
  |     +-- Cons. 2  (No persistence w/o bounds)  uses (i), Birkhoff
  |     |
  |     +-- Cons. 3  (Oscillatory necessity)       uses Cons.1 + Lemmas 1,2,3
  |           |
  |           +-- Cons. 4  (Discrete spectrum)     uses Cons.3, Koopman
  |                 |
  |                 +-- Cons. 5  (E = hbar*omega)  uses Cons.4, Bohr-Sommerfeld
  |
  +-- Cons. 6  (Partition algebra)           uses (iii)
  |     |
  |     +-- Cons. 7  (Categorical observation)     uses Cons.6, Shannon
  |
  +-- Cons. 8  (Partition coordinates n,l,m,s) uses (iii), SO(3), pi_1
  |     |
  |     +-- Cons. 9  (Shell capacity C(n)=2n^2)   uses Cons.8
  |     |
  |     +-- Cons.10  (Selection rules)             uses Cons.8, Wigner-Eckart
  |     |
  |     +-- Cons.11  (Exclusion principle)         uses Cons.8, Cons.12
  |
  +-- Cons.12  (Compression cost)            uses (iii), thermodynamics
  |     |
  |     +-- Cons.13  (Binding energy)              uses Cons.12, sub-additivity
  |
  +-- Cons.15  (Partition Lagrangian)        uses Galilean invariance
  |     |
  |     +-- Cons.16  (Propagation bound c)         uses (i), Kac
  |     |     |
  |     |     +-- Cons.18  (Lorentz invariance)    uses Cons.16, Cons.4, Ignatowsky
  |     |           |
  |     |           +-- Cons.20  (Dispersion)      uses Cons.18, Cons.19
  |     |
  |     +-- Cons.25  (Equiv. principle)            uses Cons.15, Cons.21
  |
  +-- Cons.19  (Rest frequency omega_0>0)    uses (i), Fourier
  |     |
  |     +-- Cons.21  (Rest mass m0=hbar*omega/c^2) uses Cons.20, Cons.5
  |
  +-- Cons.22  (Loschmidt / entropy increase) uses (i),(ii),(iii)
  |     |
  |     +-- Cons.23  (Mass as non-actualisation)   uses Cons.21, Cons.22
  |     |
  |     +-- Cons.24  (E=mc^2)                      uses Cons.20, Cons.21
  |     |
  |     +-- Cons.28  (Triple entropy equiv.)       uses Cons.22, S-defs
  |           |
  |           +-- Cons.29  (Ideal gas PV=NkT)      uses Cons.28, Cons.4
  |           |
  |           +-- Cons.31  (Universal transport)   uses Cons.6, linear response
  |                 |
  |                 +-- Cons.32  (Partition extinction) uses Cons.31
  |                 |
  |                 +-- Cons.33  (Wiedemann-Franz)      uses Cons.31, Sommerfeld
  |
  +-- Cons.26  (Charge emergence)            uses (iii), Cons.7
  |     |
  |     +-- Cons.27  (Charge quantisation)         uses Cons.26, (iii)
  |     |
  |     +-- Cons.43  (Coulomb envelope)            uses Cons.26, divergence-free
  |           |
  |           +-- Cons.44  (Aufbau)                uses Cons.8, Cons.11, Cons.43
  |                 |
  |                 +-- Cons.45  (Slater shielding) uses Cons.44, radial integrals
  |                 |
  |                 +-- Cons.46  (Atomic emptiness) uses Cons.8, Cons.22
  |
  +-- Cons.34  (Continuous embedding)        uses (iii), Chentsov
        |
        +-- Cons.35  (Virtual substates)          uses Cons.34
        |
        +-- Cons.36  (Sub-nucleon)                uses Cons.34, Cons.35
        |
        +-- Cons.37  (Nucleon oscillator)         uses Cons.3, Parts I-II
              |
              +-- Cons.40  (Nuclear radius A^1/3) uses Cons.12 at nuclear scale
              |
              +-- Cons.42  (Magic numbers)        uses Cons.9, spin-orbit
\end{verbatim}
\end{center}

Maximum depth from Axiom to any Consequence: \textbf{5 steps}.
No Consequence introduces a new postulate.
